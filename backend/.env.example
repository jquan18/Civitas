# ============================================
# NOTICE: Environment variables have been moved to the repository root
# ============================================
#
# Please configure your environment variables in:
# /civitas/.env (at the repository root)
#
# This file is kept for reference only.
# See the root .env file for all frontend and backend configuration.
#
# The backend requires these variables:
# - PORT
# - NODE_ENV
# - FRONTEND_URL
# - NETWORK_MODE (mainnet | testnet, default: testnet)
# - BASE_MAINNET_RPC_URL or BASE_TESTNET_RPC_URL (or BASE_RPC_URL as fallback)
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - KEEPER_PRIVATE_KEY (optional)
